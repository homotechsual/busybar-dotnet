# Generated API reference docs — regenerate via `npm run generate-api-docs`
# (see ../scripts/generate-api-docs.ps1). Not committed: fully reproducible from
# the library's XML doc comments, and would otherwise conflict with concurrent
# doc-comment work landing on the library itself.
/docs/api

# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
